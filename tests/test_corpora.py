"""
Test cases for the index functionality, including integration with some
concrete corpus objects.

"""
import hyperreal


# def test_indexing(tmp_path, corpus_class, corpus_args):
#     """Test that all builtin corpora can be successfully indexed and queried."""
#     i = hyperreal.index.Index(tmp_path / corpus.CORPUS_TYPE, corpus)
#     i.index()


# Test creation of a new model on an index

# Test modification of a previous index
